import React from "react";

/*
 * Team button static component for displaying instance of button
 * Use button type to display button
 */
const TeamButton = () => {
  return (
    <button type="button" class="btn btn-secondary">
      Left
    </button>
  );
};

/*
 * Export class to allow other classes to use TeamButton
 */
export default TeamButton;
